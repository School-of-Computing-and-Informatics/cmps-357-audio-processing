# Flask Configuration
# Set to 'true' to enable debug mode (only for development)
# WARNING: Never enable debug mode in production!
FLASK_DEBUG=false

# Flask will run on port 5000 by default
# You can customize this in app.py if needed
